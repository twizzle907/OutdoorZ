@model TravelEZ.Models.ListUserViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Users</h2>

@*@model Models.GroupedUserViewModel
    @{
        ViewBag.Title = "Index";
    }*@
<div>
    @*@foreach (var user in Model.Admins)
        {
            <p>
                <strong>@user.Username | @user.Roles </strong>
            </p>
        }*@

    @*@foreach (var user in Model.Users)
    {
        <p>
            @user.Email
        </p>
    }*@

    @*<script type="text/javascript">
    function Submit() {
        if (confirm("Are you sure you want to ban this user?")) {
            document.getElementById('anchortag').href += "?isTrue=true";
        } else {
            document.getElementById('anchortag').href += "?isTrue=false";
        }
        return true;
    }
    </script>*@



    <table class="table">
        <tr>
            <th>
                User Email Address
            </th>
            <th>
            </th>
        </tr>


        @foreach (var item in Model.Users)
        {
            //{ ViewBag.UserID = item.Id; }   
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.Id })*@
                    @*@Html.Action(actionName: "banUser", controllerName: "Account", routeValues: new { userId = item.Id}) |*@
                    @Html.ActionLink(linkText: "Ban", 
                                     actionName: "banUser", 
                                     controllerName: "Account", 
                                     routeValues: new { userId = item.Id }, 
                                     htmlAttributes: null) | @*, new { @onclick = "return Submit();", id = "anchortag" })*@
                    @*@Html.ActionLink("Ban", "Details", new { id = item.Id })*@@*, new { id = item.Id, Email = item.Email })*@
                    @Html.ActionLink("Unban", "Delete", new { id = item.Id })
                </td>
                @*<td>
                    @Html.DisplayFor(modelItem => item.isBanned)
                </td>*@
                @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>*@
            </tr>
        }

    </table>
</div>




@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>*@
